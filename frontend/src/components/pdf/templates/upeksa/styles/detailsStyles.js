import { StyleSheet } from '@react-pdf/renderer';

export const detailsStyles = StyleSheet.create({
    name: {
        marginBottom: 5
    },
    email: {
        marginBottom: 5
    },
    mobileWrapper: {
        marginBottom: 5
    },
    birthDateWrapper: {
        marginBottom: 5
    }
})